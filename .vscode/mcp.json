{
  "servers": {
    "siliconflow-images": {
      "type": "stdio",
      "command": "/var/www/bin/siliconflow-image-mcp",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "WORKSPACE_ROOT": "${workspaceFolder}"
      }
    }
  }
}
